    // ifstream fileMap(argv[1]);
    // string lineMap;
    
    // ifstream fileCommand(argv[2]);
    // string lineCommand;

    // if (fileMap.is_open()){
    //     while(getline(fileMap, lineMap)) {
    //         cout << lineMap << endl;
    //     }
    // } else {
    //     cout << "ERRO: Nao foi possivel abrir o arquivo" << endl;
    // }


    // if (fileCommand.is_open()){
    //     while(getline(fileCommand, lineCommand)) {
    //         cout << lineCommand << endl;
    //     }
    // } else {
    //     cout << "ERRO: Nao foi possivel abrir o arquivo" << endl
    // }

    // fileMap.close;
    // fileCommand.close;




    // char ch;
    // fstream fin("file", fstream::in);
    // fin >> noskipws;
    // while (fin >> noskipws >> ch) {
    //     cout << ch;
    // }


    // cout << linhas << endl;
    // cout << colunas << endl;

    //Printa a matriz
    // int i, j;
    // for(i=0; i<linhas; i++) {
	// 	for(j=0; j<colunas; j++) { 
	// 		printf("%d ", MapMatrix[i][j]);
	// 	}
	// 	printf("\n");
	// }
    // printf("\n");

    //Printa a matriz
    // for(i=0; i<linhas; i++) {
	// 	for(j=0; j<colunas; j++) { 
	// 		cout << MapMatrix[i][j];
	// 	}
	// 	cout << endl;
	// }
    // cout << endl;  

    // cout << MapMatrix[9][4];